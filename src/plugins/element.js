import Vue from "vue"
import {
    Input,
    Tree,
    Button,
    Select,
    Option,
    Popover,
    Table,
    TableColumn
} from "element-ui"

Vue
    .use(Input)
    .use(Tree)
    .use(Button)
    .use(Select)
    .use(Option)
    .use(Popover)
    .use(Table)
    .use(TableColumn)